import React from 'react';
import Logo from '../../Logo/Logo';
import './Toolbar.css';

const Toolbar = (props)=> (
    <header className='Toolbar'>
        Menu
        <Logo />
    </header>
);

export default Toolbar;